{
  "name": "@freeagent/tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Protocol-client TUI for FreeAgent. Talks to FreeAgent.Server over HTTP + SSE (see ADR 0005).",
  "scripts": {
    "start": "bun run src/tui.tsx",
    "tui": "bun run src/tui.tsx",
    "smoke": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.16",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@opentui/core": "^0.3.1",
    "@opentui/react": "^0.3.1",
    "react": "^19.2.7"
  }
}
